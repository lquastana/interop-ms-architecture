{
  "name": "pattern-flow",
  "version": "1.0.0",
  "description": "A JavaScript library that implements the Messaging Patterns interface.",
  "main": "index.ts",
  "scripts": {
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^0.10.3",
    "dotenv": "^16.0.3",
    "mongoose": "^7.1.1",
    "simple-hl7": "^3.2.4"
  },
  "devDependencies": {
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}
